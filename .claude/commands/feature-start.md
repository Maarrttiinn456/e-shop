Začínám novou feature: $ARGUMENTS

Proveď:
1. `git checkout develop && git pull`
2. `git checkout -b feature/$ARGUMENTS`
3. Potvrď mi název aktivní větve: `git branch --show-current`

Pak se zeptej: co konkrétně budu na této feature implementovat?
